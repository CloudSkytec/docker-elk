version: '3'

services:

  elasticsearch:
    build:
      context: elasticsearch/
      args:
        ELK_VERSION: $ELK_VERSION
    volumes:
      - ./elasticsearch/config/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml:ro
    ports:
      - "9200:9200"
      - "9300:9300"
    labels:
      aliyun.logs.elasticsearch: stdout
    environment:
      ES_JAVA_OPTS: "-Xmx256m -Xms256m"
    networks:
      - elk

  pilot:
    image: registry.cn-hangzhou.aliyuncs.com/acs-sample/log-pilot:latest
    environment:
      ELASTICSEARCH_HOST: elasticsearch
      ELASTICSEARCH_PORT: 9200
      LOGGING_OUTPUT: elasticsearch
      PILOT_TYPE: fluentd
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /:/host:ro
    networks:
      - elk
    depends_on:
      - elasticsearch

  kibana:
    build:
      context: kibana/
      args:
        ELK_VERSION: $ELK_VERSION
    volumes:
      - ./kibana/config/:/usr/share/kibana/config:ro
    ports:
      - "5601:5601"
    labels:
      aliyun.logs.kibana: stdout
    networks:
      - elk
    depends_on:
      - elasticsearch

networks:

  elk:
    driver: bridge